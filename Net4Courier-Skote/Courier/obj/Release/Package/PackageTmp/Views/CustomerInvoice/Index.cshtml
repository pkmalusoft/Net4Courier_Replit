@model Net4Courier.Models.CustomerInvoiceSearch
@{
    //Layout = null;
    ViewBag.Title = "List";
    ViewBag.pTitle = "Customer Invoice";
    ViewBag.pageTitle = "Customer Invoice";
    Layout = "~/Views/Shared/_Layout.cshtml";

    var isadd = true;
    var ismodify = true;
    var isdelete = true;
    var isprint = true;
    var isview = true;
    string path = "/CustomerInvoice/Index";

    if (Convert.ToInt32(Session["UserRoleID"]) > 1)
    {
        Net4Courier.Models.SourceMastersModel obj = new Net4Courier.Models.SourceMastersModel();
        isadd = obj.GetAddpermission(Convert.ToInt32(Session["UserRoleID"]), path);
        ismodify = obj.GetModifypermission(Convert.ToInt32(Session["UserRoleID"]), path);
        isdelete = obj.GetDeletepermission(Convert.ToInt32(Session["UserRoleID"]), path);
        isprint = obj.GetPrintpermission(Convert.ToInt32(Session["UserRoleID"]), path);
        isview = obj.GetViewpermission(Convert.ToInt32(Session["UserRoleID"]), path);

    }
}


 
<script type="text/javascript">
    function ShowReceipt(InvoiceId,CustomerId) {
        debugger;
        //GetInvoiceReceipt
        var url = '';
        $.ajax({
            type: "GET",
            url: "/CustomerInvoice/GetInvoiceReceipt",
            datatype: "Json",
            data: { 'InvoiceId': InvoiceId ,'CustomerId':CustomerId},
            success: function (response) {
                debugger;
                var receiptid = response;
                if (receiptid == 0) {
                    url = '/CustomerReceipt/Create/0';
                }
                else {
                    url = '/CustomerReceipt/Create/' + receiptid;
                }
                window.open(url, '_blank').focus();


            }
        });



    }
    $(document).ready(function () {
        var startdate = '@Session["FyearFrom"].ToString()';
        var enddate = '@Session["FyearTo"].ToString()';
        var sd = new Date(startdate.split('-')[1] + '-' + startdate.split('-')[0] + '-' + startdate.split('-')[2]);
        var ed = new Date(enddate.split('-')[1] + '-' + enddate.split('-')[0] + '-' + enddate.split('-')[2]);




         $('#dataTables-example tfoot th').each(function () {
                var title = $(this).text();
                $(this).html('<input type="text" style="width:100%" />');
            });
        var table = $('#dataTables-example').DataTable({ "aaSorting": [], "sPaginationType": "full_numbers" });

        table.columns().every(function () {
            var that = this;

            $('input', this.footer()).on('keyup change', function () {
                if (that.search() !== this.value) {
                    that
                        .search(this.value)
                        .draw();
                }
            });
        });

        //    var table = $('#dataTables-example').dataTable({
        //        "bDestroy": true,
        //        //"order": []
        //        "aaSorting": [],
        //        "searching": false
        //    });
        //  table.columns().every(function () {
        //    var that = this;

        //    $('input', this.footer()).on('keyup change', function () {
        //        if (that.search() !== this.value) {
        //            that
        //                .search(this.value)
        //                .draw();
        //        }
        //    });
        //});

        if ('@ismodify' == 'False')
        {
            $('#dataTables-example').find('.aedit').each(function () {
                debugger;
                $(this).addClass('hide');
            });
        }
        if ('@isdelete' == 'False')
        {
            $('#dataTables-example').find('.adelete').each(function () {
                debugger;
                $(this).addClass('hide');
            });
        }
        if ('@isprint' == 'False')
        {
            $('#dataTables-example').find('.aprint').each(function () {
                debugger;
                $(this).addClass('hide');
            });
        }

    });


</script>

    @if (TempData["SuccessMsg"] != null)
    {
        <script type="text/javascript">
                  $(document).ready(function () {
                     // $.notify("@TempData["SuccessMsg"]", "error");
                 });
        </script>
    }
    @if (TempData["ErrorMsg"] != null)
    {
        <script type="text/javascript">
                  $(document).ready(function () {
                      //$.notify("@TempData["ErrorMsg"]", "error");
                 });
        </script>
    }

@using (Html.BeginForm())
{
    @Html.AntiForgeryToken()
  
<div class="card">
    <div class="card-body">
        <div class="row">
           <div class="col-md-2">
                    <div class="mb-2">
                        <label class="col-form-label">From Date</label>
                        <div class="docs-datepicker">
                            <div class="input-group">
                                <input type="text" class="form-control docs-date" name="FromDate" id="FromDate" value="@Model.FromDate.ToShortDateString()"
                                       placeholder="Pick a From Date" autocomplete="off">
                                <button type="button"
                                        class="btn btn-secondary docs-datepicker-trigger"
                                        disabled>
                                    <i class="mdi mdi-calendar" aria-hidden="true"></i>
                                </button>
                            </div>
                            <div class="docs-datepicker-container"></div>
                        </div>
                    </div>
                </div>
                <div class="col-md-2">
                    <div class="mb-2">
                        <label class="col-form-label">To Date</label>
                        <div class="docs-datepicker">
                            <div class="input-group">
                                <input type="text" class="form-control docs-date" name="ToDate" id="ToDate" data-date-format="dd/MM/yyyy" data-date-autoclose="true" data-provide="datepicker" value="@Model.ToDate.ToShortDateString()"
                                    placeholder="Pick a date" autocomplete="off">
                                <button type="button" class="btn btn-secondary docs-datepicker-trigger" disabled>
                                    <i class="mdi mdi-calendar" aria-hidden="true"></i>
                                </button>
                                </div>
                            <div class="docs-datepicker-container"></div>
                        </div>
                    </div>
                </div>
            <div class="col-md-2 search_field">
             <div class="mb-2">
                <label for="FromDate" class="col-form-label">Search Invoice No.</label>
                @Html.TextBoxFor(mode => mode.InvoiceNo, new { @class = "form-control text-right ora_border" })
            </div>
            </div>
            <div class="col-md-2">
                                <div class="mb-2 mt-2">
                                    <button type="submit" class="btn btn-success waves-effect waves-light filter btn_top">
                                        <i class="bx bx-revision mt-1" style="font-size: 21px;"></i>
                                    </button>
                                </div>
                            </div>
      

            <div class="col-md-4 text-md-end">
                <div class="mb-2 mt-2">
                    @if (@Net4Courier.Models.CommonFunctions.CheckCreateEntryValid() == false || !isadd)
                    {
                        <a class="btn btn-primary waves-effect waves-light btn_top" href="/CustomerInvoice/Create" disabled><i class="mdi mdi-plus me-1"></i> New Customer Invoice</a>
                        <a class="btn btn-primary waves-effect waves-light btn_top" href="/CustomerInvoice/Create" disabled><i class="mdi mdi-plus me-1"></i> New Customer Invoice</a>

                    }
                    else
                    {
                        <a class="btn btn-primary waves-effect waves-light btn_top" href="/CustomerInvoice/MultipleInvoice" disabled><i class="mdi mdi-plus me-1"></i>New Multiple Invoice</a>
                        <a class="btn btn-primary waves-effect waves-light btn_top" href="/CustomerInvoice/Create"><i class="mdi mdi-plus me-1"></i> New Customer Invoice</a>
                    }

                </div>
            </div>
        </div>
    </div>
</div>

    <div class="card">
        <div class="card-body">
                  <table class="table table-bordered mb-0" id="datatable" style="width:100%">
                <thead class="table-light">
                    <tr>
                        <th>
                            Invoice No.
                        </th>
                        <th>
                            Invoice Date
                        </th>
                        <th>
                            Customer Name
                        </th>
                        <th>Total AWB</th>
                        <th>Invoice Amount</th>
                        <th></th>
                    </tr>
                </thead>
                
                @{Html.RenderPartial("Table", Model);}
            </table>
        </div>
    </div>
}

<script>
        $(document).ready(function () {
            $("#importshipment").addClass("active");
            @if(TempData["toasterMessage"] != null)
            {
                @:toasterMessage("@TempData["toasterMessage"].ToString()");
            }
        });
        function deleteExport(id, controller) {
            debugger;
            swal({
                title: "Are you sure?",
                text: "You will not be able to recover this " + controller,
                type: "warning",
                showCancelButton: true,
                confirmButtonClass: "btn-danger",
                confirmButtonText: "Yes, delete it!",
                cancelButtonText: "No, cancel plx!",
                closeOnConfirm: false,
                closeOnCancel: false
            },
                function (isConfirm) {
                    if (isConfirm) {
                        $.ajax({
                            type: "POST",
                            datatype: "html",
                            url: "Delete?id=" + id.toString(),
                            data: id,
                            success: function (data) {
                                debugger;
                                swal({ title: "Deleted!", text: controller + " has been deleted.", type: "success" }, function () { window.location = window.location.href; });
                            }
                        });
                    } else {
                        swal("Cancelled", controller + " is safe :)", "error");
                    }
                });

        }
</script>

