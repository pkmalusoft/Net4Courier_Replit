@model IEnumerable<Net4Courier.Models.OtherChargeVM>

@{
    ViewBag.Title = "List";
        ViewBag.pTitle = "Other Charge";
    ViewBag.pageTitle = "Other Charge";
    Layout = "~/Views/Shared/_Layout.cshtml";
    var isadd = true;
    var ismodify = true;
    var isdelete = true;

    string path = Request.Url.AbsolutePath.ToLower();

    if (Convert.ToInt32(Session["UserRoleID"]) > 1)
    {
        Net4Courier.Models.SourceMastersModel obj = new Net4Courier.Models.SourceMastersModel();
        isadd = obj.GetAddpermission(Convert.ToInt32(Session["UserRoleID"]), path);
        ismodify = obj.GetModifypermission(Convert.ToInt32(Session["UserRoleID"]), path);
        isdelete = obj.GetDeletepermission(Convert.ToInt32(Session["UserRoleID"]), path);

    }
}


<script type="text/javascript">
    $(document).ready(function () {

        $('#dataTables-example tfoot th').each(function () {
            var title = $(this).text();
            $(this).html('<input type="text" />');
        });


        var table = $('#dataTables-example').DataTable({ "sPaginationType": "full_numbers" });

        table.columns().every(function () {
            var that = this;

            $('input', this.footer()).on('keyup change', function () {
                if (that.search() !== this.value) {
                    that
                        .search(this.value)
                        .draw();
                }
            });
        });
    });


 </script>


        @if (TempData["SuccessMSG"] != null)
        {
            <script type="text/javascript">
         $(document).ready(function () {
            //$.notify("@TempData["SuccessMSG"] ", "success");
          });
            </script>
        }
    <div class="card">
        <div class="card-body">
                <div class="col-md-12 mb-3 text-md-end">
<a  class="btn btn-primary waves-effect waves-light text-md-end" href="/OtherCharge/Create"><i class="mdi mdi-plus me-1"></i> New Other Charge

</a>
</div>
       <table class="table table-bordered mb-0" id="datatable" style="width:100%">
                <thead class="table-light">
                    <tr>
                        <th>
                           Other Charge Name
                        </th>
                        <th>
                            Chart of Account
                        </th>
                        <th>
                            Type %
                        </th>
                        <th>
                           Amount
                        </th>
                        <th>Reimbursement</th>
                        <th>Tax Applicable</th>
                        <th>
                            Action
                        </th>

                    </tr>
                </thead>


                <tbody>
                    @foreach (var item in Model)
                    {
                    <tr>
                        <td class="tdtarget">
                            @Html.DisplayFor(modelItem => item.OtherCharge1)
                        </td>
                        <td class="tdtarget">
                            @Html.DisplayFor(modelItem => item.AcHead)
                        </td>
                        <td>
                            @if (item.TypePercent == true)
                            {<i class="fa fa-check"></i>}

                        </td>
                        <td class="text-right">
                            @Net4Courier.Models.CommonFunctions.GetDecimalFormat(@item.SetAmount, "2")
                        </td>
                        <td>
                            @if (item.Reimbursement == true)
                            {<i class="fa fa-check"></i>}

                        </td>
                        <td>
                            @if (item.TaxApplicable == true)
                            {<i class="fa fa-check"></i>}

                        </td>

                        <td>
                         <div class="d-flex gap-3">
                            @*@Html.ActionLink("Edit", "Edit", new { id=item.RevenueTypeID }) |*@
                            @* @Html.ActionLink("Details", "Details", new { id=item.RevenueTypeID }) |*@
                            @* @Html.ActionLink("Delete", "Delete", new { id=item.RevenueTypeID })*@
                            @if (ismodify)
                            {
                                <a href="@Url.Action("Edit", new { id = item.OtherChargeID })" class="text-success"><i class="mdi mdi-pencil font-size-18"></i></a>
                            } 
                            @* <a href="@Url.Action("Delete", new { id = item.RevenueTypeID })"><i class="fa fa-times"></i></a>*@
                            @if (isdelete)
                            {
                                <a href="#" onclick="Delete(ID=@item.OtherChargeID)" class="text-danger"><i class="mdi mdi-delete font-size-18"></i></a>
                            }
                            </div>
                        </td>
                    </tr>
                    }
                </tbody>

            </table>
        </div>
    </div>

    <script type="text/javascript">
        // function Delete(ID) {
        //     if (confirm("Are you sure, You want to Delete this OtherCharge?")) {
        //         window.location.href = "/OtherCharge/DeleteConfirmed/?id=" + ID;
        //     }
        // }

        function Delete(ID) {
        Swal.fire({ title: "Are you sure?", text: "Do you want to delete this?", icon: "warning", showCancelButton: !0, confirmButtonColor: "#34c38f", cancelButtonColor: "#f46a6a", confirmButtonText: "Yes, delete it!" }).then(
            function (t) {
                if (t.value) {
                    // $.ajax({
                    //     type: "POST",
                    //     url: '/OtherCharge/DeleteConfirmed',
                    //     datatype: "html",
                    //     data: {
                    //         'id': ID
                    //     },
                    //     success: function (response) {
                    //         if (response.status == "OK") {
                    //             Swal.fire("Delete Status!", response.message, "success");
                    //             window.location.reload();
                    //         }
                    //         else {
                    //             Swal.fire("Delete Status!", response.message, "error");
                    //         }
                    //     }
                    // });
                }
            });

            }
</script>
