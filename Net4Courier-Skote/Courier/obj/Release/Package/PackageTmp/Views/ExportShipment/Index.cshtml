@model Net4Courier.Models.ExportShipmentSearch
@{
   ViewBag.Title = "List";
    ViewBag.pTitle = "Manage Manifest";
    ViewBag.pageTitle = "Outbound";
    Layout = "~/Views/Shared/_Layout.cshtml";
    
    var isadd = true;
    var ismodify = true;
    var isdelete = true;

    string path = "/ExportShipment/Index";

    if (Convert.ToInt32(Session["UserRoleID"]) > 1)
    {
        Net4Courier.Models.SourceMastersModel obj = new Net4Courier.Models.SourceMastersModel();
        isadd = obj.GetAddpermission(Convert.ToInt32(Session["UserRoleID"]), path);
        ismodify = obj.GetModifypermission(Convert.ToInt32(Session["UserRoleID"]), path);
        isdelete = obj.GetDeletepermission(Convert.ToInt32(Session["UserRoleID"]), path);

    }
}


<script type="text/javascript">


    //$(function () {
    //    $('#FromDate').datetimepicker({ format: 'DD-MM-YYYY' });

    //});

    //$(function () {
    //    $('#ToDate').datetimepicker({ format: 'DD-MM-YYYY' });

    //    $('#ToDate').datepicker("setDate", new Date());
    //});

    //$(function () {
    //    $('#txtpickuptime').datetimepicker({ format: 'DD-MM-YYYY HH:mm' });
    //});

    var table = $('#datatable-1').DataTable();

</script>
<script type="text/javascript">
    $(document).ready(function () {
     var startdate = '@Session["FyearFrom"].ToString()';
        var enddate = '@Session["FyearTo"].ToString()';
        var sd = new Date(startdate.split('-')[1] + '-' + startdate.split('-')[0] + '-' + startdate.split('-')[2]);
        var ed = new Date(enddate.split('-')[1] + '-' + enddate.split('-')[0] + '-' + enddate.split('-')[2]);
        
        $('#dataTables-example tfoot th').each(function () {
            var title = $(this).text();
            $(this).html('<input type="text" style=width:100% />');
        });


        var table = $('#dataTables-example').DataTable({ "aaSorting": [], "sPaginationType": "full_numbers" });

        table.columns().every(function () {
            var that = this;

            $('input', this.footer()).on('keyup change', function () {
                if (that.search() !== this.value) {
                    that
                        .search(this.value)
                        .draw();
                }
            });
        });
    });



</script>


    @if (TempData["SuccessMsg"] != null)
    {
        <script type="text/javascript">
                  $(document).ready(function () {
                      //$.notify("@TempData["SuccessMsg"]", "success");
                 });
        </script>
    }
    @if (TempData["ErrorMsg"] != null)
    {
        <script type="text/javascript">
                  $(document).ready(function () {
                      //$.notify("@TempData["ErrorMsg"]", "error");
                 });
        </script>
    }

@using (Html.BeginForm("Index", "ExportShipment", FormMethod.Post, new { @id = "booksearch" }))
{

    @Html.AntiForgeryToken()
        <div class="card">
            <div class="card-body">
                <div class="row">
                    <div class="col-md-2">
                        <div class="mb-2">
                            <label class="headinglabel">From Date</label>
                            <div class="docs-datepicker">
                                <div class="input-group">
                                    <input type="text" class="form-control docs-date" name="FromDate" id="FromDate" value="@Model.FromDate.ToShortDateString()"
                                           placeholder="Pick a From Date" autocomplete="off">
                                    <button type="button"
                                            class="btn btn-secondary docs-datepicker-trigger"
                                            disabled>
                                        <i class="mdi mdi-calendar" aria-hidden="true"></i>
                                    </button>
                                </div>
                                <div class="docs-datepicker-container"></div>
                            </div>
                        </div>
                    </div>
                    <div class="col-md-2">
                        <div class="mb-2">
                            <label class="headinglabel">To Date</label>
                            <div class="docs-datepicker">
                                <div class="input-group">
                                    <input type="text" class="form-control docs-date" name="ToDate" id="ToDate" data-date-format="dd/MM/yyyy" data-date-autoclose="true" data-provide="datepicker" value="@Model.ToDate.ToShortDateString()"
                                           placeholder="Pick a date" autocomplete="off">
                                    <button type="button"
                                            class="btn btn-secondary docs-datepicker-trigger"
                                            disabled>
                                        <i class="mdi mdi-calendar" aria-hidden="true"></i>
                                    </button>
                                </div>
                                <div class="docs-datepicker-container"></div>
                            </div>
                        </div>
                    </div>
                    <div class=" col-md-2">
                        <div class="mb-2">
                            <label for="Receipt No." class="col-form-label">Search AWB No.</label>
                            @Html.TextBoxFor(mode => mode.AWBNo, new { @class = "form-control text-right ora_border" })
                        </div>
                    </div>
                    <div class="col-md-2">
                        <div class="mb-2">
                            <button type="submit" class="btn btn-success waves-effect waves-light filter btn_top reload">
                                <i class="bx bx-revision mt-1" style="font-size: 21px;"></i>
                            </button>
                        </div>
                    </div>

                    <div class="col-md-4 btn_top text-md-end">
                        @*@if (ViewBag.Token.Create)
            {
                @Html.ActionLink("Create", "Create", null, new { @class = "btn btn-primary btn-sm waves-effect waves-light" });
            }*@
                        @if (@Net4Courier.Models.CommonFunctions.CheckCreateEntryValid() == false || (!isadd))
                        {
                            <a href='/ExportShipment/CreateExport' class="btn btn-primary" id="btnCreateNew" disabled><i class="mdi mdi-plus me-1"></i> New Export Shipment</a>
                        }
                        else
                        {
                            <a href='/ExportShipment/CreateExport' class="btn btn-primary" id="btnCreateNew"><i class="mdi mdi-plus me-1"></i> New Export Shipment</a>
                        }
                        @*@Html.ActionLink("View Imports", "ViewImport", null, new { @class = "btn btn-primary btn-sm waves-effect waves-light" })

            <button type="button" style="margin-left:25px" class="btn btn-primary btn-sm waves-effect waves-light" onclick="printDiv()">Summary</button>*@
                    </div>
                </div>
            </div>
        </div>
}

    <div class="card">
        <div class="card-body">
            <table class="table table-bordered mb-0" id="datatable">
                <thead class="table-light">
                    <tr>
                        <th>Manifest Number</th>
                        <th>Manifest Date</th>
                        <th>Agent Name</th>
                        <th>Total HAWB</th>
                        <th>MAWB</th>
                        <th>MAWB Wt.</th>
                        <th>Bag</th>
                        <th>CD</th>
                        <th>Run No.</th>
                        <th>Action</th>
                    </tr>
                </thead>
              

            @foreach (var item in Model.Details)
            {
            <tr>
                <td>
                    @Html.DisplayFor(modelItem => item.ManifestNumber)
                </td>
                <td>
                    @Net4Courier.Models.CommonFunctions.GetShortDateFormat(@item.ManifestDate)
                </td>
                <td>

                    <span class="" title="Created Detail">@Html.DisplayFor(modelItem => item.FAgentName) </span> <br />



                    <span class="colorblue" title="Modified Detail">@Html.DisplayFor(modelItem => item.AgentName)</span>
                </td>
                <td>
                    <span style="text-wrap:initial">
                        @Html.DisplayFor(modelItem => item.AWBNumbers)
                    </span>
                </td>
                <td>
                    <span style="text-wrap:initial">
                        @Html.DisplayFor(modelItem => item.MAWB)
                    </span>
                </td>
                <td>
                    <span style="text-wrap:initial">
                        @Html.DisplayFor(modelItem => item.MAWBWeight)
                    </span>
                </td>
                <td>
                    <span style="text-wrap:initial">
                        @Html.DisplayFor(modelItem => item.Bags)
                    </span>
                </td>
                <td>
                    <span style="text-wrap:initial">
                        @Html.DisplayFor(modelItem => item.CD)
                    </span>
                </td>
                <td>
                    <span style="text-wrap:initial">
                        @Html.DisplayFor(modelItem => item.RunNo)
                    </span>
                </td>
                <td>
                    <div class="d-flex gap-3">
                        <a href="@Url.Action("CreateExport", new { id = item.ID })" class="text-success"><i class="mdi mdi-pencil font-size-18"></i></a>
                        <a href="@Url.Action("Details", new { id = item.ID })" class="text-primary"><i class="mdi mdi-eye font-size-18"></i></a>
                        <a href="#" onclick="Delete(ID=@item.ID)" class="text-danger"><i class="mdi mdi-delete font-size-18"></i></a>
                        <a class="aprint text-secondary" target="_blank" href="ExportShipmentPrint?id=@item.ID" title="Import Shipment Printing"><i class="mdi mdi-printer font-size-18"></i></a>
                        <a class="aprint" target="_blank" href="ExportShipmentXML?id=@item.ID" title="Manifest XML Downloading "><i class="mdi mdi-download font-size-18"></i></a>
                        <a class="aprint text-success" target="_blank" href="ManifestDownload?id=@item.ID" title="Manifest Excel Downloading "><i class="mdi mdi-file-excel font-size-18"></i></a>
                        @*<a href="@Url.Action("ExportShipmentReport", new { id = item.ID })"><i class="fa fa-print"></i></a> |*@
                    </div>
                </td>
            </tr>
            }

        </table>
    </div>
</div>
<script>
        $(document).ready(function () {
            $("#importshipment").addClass("active");
            @if(TempData["toasterMessage"] != null)
            {
                @:toasterMessage("@TempData["toasterMessage"].ToString()");
            }
        });

    // function Delete(ID) {
    //     if (confirm("Are you sure, You want to Delete Export Manifest?")) {
    //         window.location.href = "/ExportShipment/DeleteConfirmed/?id=" + ID;
    //     }
    // }
      function Delete(ID) {
        Swal.fire({ title: "Are you sure?", text: "Do you want to delete this?", icon: "warning", showCancelButton: !0, confirmButtonColor: "#34c38f", cancelButtonColor: "#f46a6a", confirmButtonText: "Yes, delete it!" }).then(
            function (t) {
                if (t.value) {

                     $.ajax({
                         type: "POST",
                         url: '/ExportShipment/DeleteConfirmed',
                         datatype: "html",
                         data: {
                             'id': ID
                         },
                         success: function (response) {
                             if (response.Status == "OK") {
                                 Swal.fire("Delete Status!", response.Message, "success");
                                 window.location.reload();
                             }
                             else {
                                 Swal.fire("Delete Status!", response.Message, "error");
                             }
                         }
                     });

                }
            });
      }

    function deleteExport(id, controller) {
        if (confirm("Are you sure, You want to Delete this Export Manifest Entry ?")) {
            window.location.href = "/ExportShipment/Delete?id=" + id;
        }
            //debugger;
            //swal({
            //    title: "Are you sure?",
            //    text: "You will not be able to recover this " + controller,
            //    type: "warning",
            //    showCancelButton: true,
            //    confirmButtonClass: "btn-danger",
            //    confirmButtonText: "Yes, delete it!",
            //    cancelButtonText: "No, cancel plx!",
            //    closeOnConfirm: false,
            //    closeOnCancel: false
            //},
            //    function (isConfirm) {
            //        if (isConfirm) {
            //            $.ajax({
            //                type: "POST",
            //                datatype: "html",
            //                url: "Delete?id=" + id.toString(),
            //                data: id,
            //                success: function (data) {
            //                    debugger;
            //                    swal({ title: "Deleted!", text: controller + " has been deleted.", type: "success" }, function () { window.location = window.location.href; });
            //                }
            //            });
            //        } else {
            //            swal("Cancelled", controller + " is safe :)", "error");
            //        }
            //    });

        }
</script>

