@model Net4Courier.Models.BankModal
@using System.Data
@{
    Layout = "~/Views/Shared/_Layout.cshtml";
    ViewBag.Title = "BankReconciliation";
}

<div class="row" id="DetailReport">
    <div class="col-12">
        <div class="iq-card">
            <div class="iq-card-header d-flex justify-content-between">
                <div class="iq-header-title">
                    <h4 class="card-title">Bank Reconciliation</h4>
                </div>
            </div>
            <div class="iq-card-body">
               
                    @using (Html.BeginForm("Save", "BankReconciliation"))
                    {
                        @Html.TextBoxFor(m => m.chkProduct)

                        var i = 0;
                        //foreach (CsvRecordsViewModel item in Model.ResultViewModel)
                        //{

                        //}
                        @Html.HiddenFor(m=> m.ResultViewModel)
                        foreach (var testCollection in Model.ResultViewModel)
                        {
                <input type="text" id="BankModal.ResultViewModel[@i].IsCleared" name="BankModal.ResultViewModel[@i].IsCleared" value="@testCollection.IsCleared" />
                <br />
                i++;
            }

                        @*for (var i = 0; i < Model.ResultViewModel.Count; i++)
                        {
                            @Html.LabelFor(x => x.ResultViewModel[i].ID)
                            @Html.TextBox(string.Format("obj.ResultViewModel[{0}].ID", i), Model.ResultViewModel[i].ID)

                            @Html.LabelFor(x => x.ResultViewModel[i].ID)
                            @Html.TextBox(string.Format("obj.ResultViewModel[{0}].ID", i), Model.ResultViewModel[i].ID)
                        <br />
                        }*@
                        <input type="submit" value="Save" />

                    }
               
            </div>
        </div>
    </div>
</div>



