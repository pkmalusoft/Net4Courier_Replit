@model Net4Courier.Models.AWBPrepaidSearch

@{
   ViewBag.Title = "List";
    ViewBag.pTitle = "Customer AWB";
    ViewBag.pageTitle = "Manage AWB Stock";
    Layout = "~/Views/Shared/_Layout.cshtml";
    var isadd = true;
    var ismodify = true;
    var isdelete = true;
    var isprint = true;
    var isview = true;
    string path = "/AWBPrepaid/Index";
    if (Convert.ToInt32(Session["UserRoleID"]) > 1)
    {
        Net4Courier.Models.SourceMastersModel obj = new Net4Courier.Models.SourceMastersModel();
        isadd = obj.GetAddpermission(Convert.ToInt32(Session["UserRoleID"]), path);
        ismodify = obj.GetModifypermission(Convert.ToInt32(Session["UserRoleID"]), path);
        isdelete = obj.GetDeletepermission(Convert.ToInt32(Session["UserRoleID"]), path);
        isprint = obj.GetPrintpermission(Convert.ToInt32(Session["UserRoleID"]), path);
        isview = obj.GetViewpermission(Convert.ToInt32(Session["UserRoleID"]), path);

    }
}



<script type="text/javascript">
    //$(function () {
    //    $('#FromDate').datetimepicker({ format: 'DD-MM-YYYY' });

    //});

    //$(function () {
    //    $('#ToDate').datetimepicker({ format: 'DD-MM-YYYY' });

    //});
    $(document).ready(function () {
        var startdate = '@Session["FyearFrom"].ToString()';
        var enddate = '@Session["FyearTo"].ToString()';
        var sd = new Date(startdate.split('-')[1] + '-' + startdate.split('-')[0] + '-' + startdate.split('-')[2]);
        var ed = new Date(enddate.split('-')[1] + '-' + enddate.split('-')[0] + '-' + enddate.split('-')[2]);

        $('#dataTables-example tfoot th').each(function () {
            var title = $(this).text();
            $(this).html('<input type="text" style=width:100% />');
        });


        var table = $('#dataTables-example').DataTable({ "sPaginationType": "full_numbers" });

        table.columns().every(function () {
            var that = this;

            $('input', this.footer()).on('keyup change', function () {
                if (that.search() !== this.value) {
                    that
                        .search(this.value)
                        .draw();
                }
            });
        });
    });

</script>

@using (Html.BeginForm("Index", "AWBPrepaid", FormMethod.Post, new { @id = "booksearch" }))
{
    <div class="card">
        <div class="card-body">
        <div class="row">
                   <div class="col-md-2">
                    <div class="mb-2">
                        <label class="headinglabel">From Date</label>
                        <div class="docs-datepicker">
                            <div class="input-group">
                                <input type="text" class="form-control docs-date" name="FromDate" id="FromDate" value="@Convert.ToDateTime(@Model.FromDate).ToShortDateString()"
                                       placeholder="Pick a From Date" autocomplete="off">
                                <button type="button"
                                        class="btn btn-secondary docs-datepicker-trigger"
                                        disabled>
                                    <i class="mdi mdi-calendar" aria-hidden="true"></i>
                                </button>
                            </div>
                            <div class="docs-datepicker-container"></div>
                        </div>
                    </div>
                </div>
                <div class="col-md-2">
                    <div class="mb-2">
                        <label class="headinglabel">To Date</label>
                        <div class="docs-datepicker">
                            <div class="input-group">
                                <input type="text" class="form-control docs-date" name="ToDate" id="ToDate" data-date-format="dd/MM/yyyy" data-date-autoclose="true" data-provide="datepicker" value="@Convert.ToDateTime(@Model.ToDate).ToShortDateString()"
                                       placeholder="Pick a date" autocomplete="off">
                                <button type="button"
                                        class="btn btn-secondary docs-datepicker-trigger"
                                        disabled>
                                    <i class="mdi mdi-calendar" aria-hidden="true"></i>
                                </button>
                            </div>
                            <div class="docs-datepicker-container"></div>
                        </div>
                    </div>
                </div>
            <div class="col-md-2">
            <div class="mb-2">
                <label class="headinglabel">Search AWB No.</label>
                @Html.TextBoxFor(model => model.DocumentNo, new { @class = "form-control ora_border" })
            </div>
            </div>
           

            <div class="col-md-2">
                   <div class="mb-2">
                        <button type="submit" class="btn btn-success waves-effect waves-light filter btn_top">
                            <i class="bx bx-search mt-1" style="font-size: 21px;"></i>
                        </button>
                    </div>
                </div>
            <div class="col-md-4 text-md-end btn_top">
          
                    @if (@Net4Courier.Models.CommonFunctions.CheckCreateEntryValid() == false || !isadd)
                    {
                         <a class="btn btn-primary waves-effect waves-light text-md-end" href="/AWBPrepaid/Create" disabled><i class="mdi mdi-plus me-1" title="Create Not allowed in the previous financial year"></i> New Customer AWB</a>
                    }
                    else
                    {
                       <a class="btn btn-primary waves-effect waves-light text-md-end" href="/AWBPrepaid/Create"><i class="mdi mdi-plus me-1"></i> New Customer AWB</a>
                    }
            </div>
        </div>
    </div>
    </div>
}

    @if (TempData["SuccessMsg"] != null)
    {

        <script type="text/javascript">
                 $(document).ready(function () {
                     $.notify("@TempData["SuccessMsg"]", "success");
                 });
        </script>
    }
        <div class="card">
        <div class="card-body">
            <table class="table table-bordered mb-0" id="datatable" style="width:100%">
                    <thead class="table-light">
                <tr>
                    <th>
                       DocumentNo
                    </th>
                    <th>
                        Trans Date
                    </th>
                    <th>
                        Reference
                    </th>
                    <th>
                       Customer Name
                    </th>
                    <th>
                        AWB No From
                    </th>
                    <th>
                        AWB No To
                    </th>
                    <th>No. of AWB</th>
                    <th>Action</th>
                </tr>
            </thead>
            

            @foreach (var item in Model.Details)
            {
                <tr>
                    <td>
                        @Html.DisplayFor(modelItem => item.Documentno)
                    </td>
                    <td>
                        @Net4Courier.Models.CommonFunctions.GetShortDateFormat(@item.TransDate)
                    </td>
                    <td>
                        @Html.DisplayFor(modelItem => item.Reference)
                    </td>
                    <td>
                        @Html.DisplayFor(modelItem => item.CustomerName)
                    </td>
                    <td>
                        @Html.DisplayFor(modelItem => item.AWBNOFrom)
                    </td>
                    <td>
                        @Html.DisplayFor(modelItem => item.AWBNOTo)
                    </td>
                    <td>
                        @Html.DisplayFor(modelItem => item.NoOfAWBs)
                    </td>
                    <td>
                    <div class="d-flex gap-3">
                        @if (ismodify)
                        {
                            <a href="@Url.Action("Create", new { id = item.PrepaidAWBID })" class="text-success"><i class="mdi mdi-pencil font-size-18"></i></a>
                        }
                        
                        @if (isprint)
                        {
                            <a href="#" class="text-secondary"><i class="mdi mdi-printer font-size-18" aria-hidden="true" onclick="funExportToPDF(@item.PrepaidAWBID)"></i></a> 
                                                                                                                                        
                            <a target="_blank" href="@Url.Action("AWBPrintReport", new { id = item.PrepaidAWBID })"> V</a>
                        }
                        
                        @if (isdelete)
                        {
                            <a href="#" onclick="Delete(ID=@item.PrepaidAWBID)" class="text-danger"><i class="mdi mdi-delete font-size-18"></i></a>
                        }
                       </div> 
                    </td>
                </tr>
            }

        </table>
    </div>
</div>
<script type="text/javascript">
      function funExportToPDF(id) {

        $.ajax({
             url: '@Url.Action("ReceiptPrint", "AWBPrepaid")',

            type: "GET",
            data: { id:id},
                    dataType: "json",
            success: function (response) {
                if (response.result == "ok") {
                    $('#frmPDF').attr('src', @Url.Content("~/ReportsPDF/") + response.path); //''

                    setTimeout(function () {
                        
                        window.open('@Url.Content("~/ReportsPDF/")' + response.path);
                      
                    }, 1000);
                } else {
                    alert(resuponse.message);
                    //hideLoading();
                }
            },
        });

        return false;
    }
</script>
<script type="text/javascript">
    function Delete(ID) {
        if (confirm("Are you sure, You want to Delete AWB Prepaid?")) {
            window.location.href = "/AWBPrepaid/DeleteConfirmed/?id=" + ID;
        }
    }
</script>
