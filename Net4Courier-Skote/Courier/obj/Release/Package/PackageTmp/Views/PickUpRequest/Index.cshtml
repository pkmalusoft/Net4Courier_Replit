@model IEnumerable<Net4Courier.Models.PickupRequestVM>

    @{
        ViewBag.Title = "List";
        ViewBag.pTitle = "Customer Booking";
        ViewBag.pageTitle = "Customer Booking";
        Layout = "~/Views/Shared/_Layout.cshtml";
    }

    <style>
        tfoot {
            display: table-header-group;
        }
      
    </style>
  
    <script type="text/javascript">


    //    $(function () {
    //        $('#FromDate').datetimepicker({ format: 'DD-MM-YYYY' });
            
    //    });

    //    $(function () {
    //        $('#ToDate').datetimepicker({ format: 'DD-MM-YYYY' });

    //        $('#ToDate').datepicker("setDate", new Date());
    //    });

    //    $(function () {
    //    $('#txtpickuptime').datetimepicker({ format: 'DD-MM-YYYY HH:mm' });
    //});
        


    </script>

    <script type="text/javascript">
        @*function LoadList() {
            debugger;

            $.ajax({
                url: '@Url.Action("Index", "PickUpRequest")',
                data: {StatusId:$('#StatusId').val(), FromDate: $('#FromDate').val(), ToDate: $('#ToDate').val() }
            }).done(function () {
                alert('Added');
            });
        }*@

     

 
        $(document).ready(function () {

            $("select").css({ "border-radius": "5px" });
             var startdate = '@Session["FyearFrom"].ToString()';
        var enddate = '@Session["FyearTo"].ToString()';
        var sd = new Date(startdate.split('-')[1] + '-' + startdate.split('-')[0] + '-' + startdate.split('-')[2]);
        var ed = new Date(enddate.split('-')[1] + '-' + enddate.split('-')[0] + '-' + enddate.split('-')[2]);

       


  $('#dataTables-example tfoot th').each(function () {
                var title = $(this).text();
                $(this).html('<input type="text" style="width:100%" />');


                //var table = $('#dataTables-example').DataTable();



            });
            //LoadList();

            //var table = $('#dataTables-example').DataTable({ "sPaginationType": none });

            //var table = $('#dataTables-example').DataTable();
            var table = $('#dataTables-example').DataTable({"aaSorting": []});
              table.draw();
 table.columns().every(function () {
            var that = this;

            $('input', this.footer()).on('keyup change', function () {
                if (that.search() !== this.value) {
                    that
                        .search(this.value)
                        .draw();
                }
            });
        });



         





         
            

               var m_names = new Array("Jan", "Feb", "Mar",
"Apr", "May", "Jun", "Jul", "Aug", "Sep",
"Oct", "Nov", "Dec");

        var d = new Date();
        var curr_date = d.getDate();
        var curr_month = d.getMonth()+1;
        var curr_year = d.getFullYear();

        var reqdate = curr_date + "-" + curr_month + "-" + curr_year;

            if ($("#FromDate").val()=="")
                $("#FromDate").val('@ViewBag.FromDate');

            if ($("#ToDate").val()=="")
                $("#ToDate").val('@ViewBag.ToDate');


       });


    </script>

    <div class="card">
        <div class="card-body">
        <div class="row">
            <div class="col-md-2 hide">
                <input type="hidden" id="PickupRequestStatusId" />
                <input type="hidden" id="InScanID" />
                <label class="lab_blue">Status</label>
                @Html.DropDownList("StatusId", new SelectList(@ViewBag.PickupRequestStatus, "CourierStatusID", "CourierStatus", @ViewBag.StatusId), "Select All", new { @class = "form-control blue_border", @value = 1, @id = "StatusId", @name = "StatusId" })
            </div>
            <div class="col-md-2">
                    <div class="mb-2">
                        <label class="headinglabel">From Date</label>
                        <div class="docs-datepicker">
                            <div class="input-group">
                                <input type="text" class="form-control docs-date" name="FromDate" id="FromDate" value="@ViewBag.FromDate"
                                       placeholder="Pick a From Date" autocomplete="off">
                                <button type="button"
                                        class="btn btn-secondary docs-datepicker-trigger"
                                        disabled>
                                    <i class="mdi mdi-calendar" aria-hidden="true"></i>
                                </button>
                            </div>
                            <div class="docs-datepicker-container"></div>
                        </div>
                    </div>
                </div>
                <div class="col-md-2">
                    <div class="mb-2">
                        <label class="headinglabel">To Date</label>
                        <div class="docs-datepicker">
                            <div class="input-group">
                                <input type="text" class="form-control docs-date" name="ToDate" id="ToDate" data-date-format="dd/MM/yyyy" data-date-autoclose="true" data-provide="datepicker" value="@ViewBag.ToDate"
                                       placeholder="Pick a date" autocomplete="off">
                                <button type="button"
                                        class="btn btn-secondary docs-datepicker-trigger"
                                        disabled>
                                    <i class="mdi mdi-calendar" aria-hidden="true"></i>
                                </button>
                            </div>
                            <div class="docs-datepicker-container"></div>
                        </div>
                    </div>
                </div>
                <div class="col-md-2">
                   <div class="mb-2">
                        <button type="button" class="btn btn-success waves-effect waves-light filter btn_top"  onclick="location.href='@Url.Action("Index", "PickupRequest")?StatusId=' + $('#StatusId').val() + '&FromDate=' + $('#FromDate').val() + '&ToDate=' + $('#ToDate').val()">
                            <i class="bx bx-search mt-1" style="font-size: 21px;"></i>
                        </button>
                    </div>
                </div>
          
                <div class="col-md-6 text-md-end btn_top">
                    <div class="mb-2">
                        @if (@Net4Courier.Models.CommonFunctions.CheckCreateEntryValid() == false)
                        {
                            <a href='/AWBCustomerBook/Create' class="btn btn-primary"><i class="mdi mdi-plus me-1"></i> New Customer Booking</a>                    
                        }
                        else
                        {
                            <a href = '/AWBCustomerBook/Create' class="btn btn-primary"><i class="mdi mdi-plus me-1"></i> New Customer Booking</a>                       
                        }
                    </div>
                </div>
        </div >
        </div>
</div>
        @if(TempData["SuccessMsg"] != null)
        {
            <script type = "text/javascript" >
                 $(document).ready(function() {
                     $.notify("@TempData["SuccessMsg"]", "success");
                        });
            </script >
        }
            

    <div class="card">
        <div class="card-body">
            <div class="table-responsive" style="padding:15px">
                <table class="table table-bordered mb-0" id="datatable" style="width:100%">
                    <thead class="table-light">
                        <tr>
                            <th >AWB</th>
                            <th>Request Date</th>
                            <th>Shipper</th>
                            <th>Consignee</th>
                            <th>Consignee City</th>
                            <th>Consignee Country</th>
                            <th>Status</th>
                            <th>Action</th>
                        </tr>
                    </thead>

                <tbody>
@foreach(var item in Model)
{

<tr>


    <td>
        @item.AWBNo
    </td>
    <td style = "text-align:right" >
        @Net4Courier.Models.CommonFunctions.GetShortDateFormat(item.EnquiryDate)
    </td >
    <td>
        @Html.DisplayFor(modelItem => item.Consignor)
    </td>

    <td>
        @Html.DisplayFor(modelItem => item.Consignee)
    </td >

    <td>
        @Html.DisplayFor(modelItem => item.ConsigneeCityName)
    </td> 
    <td>
        @Html.DisplayFor(modelItem => item.ConsigneeCountryName)
    </td>
    <td>
        @Html.DisplayFor(modelItem => item.PickupRequestStatus)
    </td>
    <td>
        @*<a href="#" title="Change Satus" onclick="ShowStatusModal('@item.EnquiryID','@item.PickupRequestStatusId','@item.ShipmentType')"><i class="fa fa-file"></i></a>  |
        <a href = "@Url.Action("Create", new { id = item.EnquiryID })"><i class="fa fa-pencil"></i></a> |-->*@
            @*<a href="@Url.Action("Delete", new { id = item.RoleID })"><i class="fa fa-times"></i></a>*@
      <a href = "#" onclick="Delete(ID=@item.InScanID)" class="text-danger"><i class="mdi mdi-delete"></i></a>
   </td>-->
</tr>
}
</tbody>

</table>
</div>        
    </section>
    <script type="text/javascript">
        // function Delete(ID) {
        //     if (confirm("Are you sure? You want to Delete PickUp Request?")) {
        //         window.location.href = "/PickUpRequest/DeleteConfirmed?id=" + ID;
        //     }
        // }
    

    function Delete(ID) {
        Swal.fire({ title: "Are you sure?", text: "Do you want to delete this?", icon: "warning", showCancelButton: !0, confirmButtonColor: "#34c38f", cancelButtonColor: "#f46a6a", confirmButtonText: "Yes, delete it!" }).then(
            function (t) {
                if (t.value) {

                    // $.ajax({
                    //     type: "POST",
                    //     url: '/PickUpRequest/DeleteConfirmed',
                    //     datatype: "html",
                    //     data: {
                    //         'id': ID
                    //     },
                    //     success: function (response) {
                    //         if (response.status == "OK") {
                    //             Swal.fire("Delete Status!", response.message, "success");
                    //             window.location.reload();
                    //         }
                    //         else {
                    //             Swal.fire("Delete Status!", response.message, "error");
                    //         }
                    //     }
                    // });

                }
            });

    }
</script>