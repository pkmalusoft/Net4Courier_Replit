@model Net4Courier.Models.MCPaymentPrintSearch

@{
    ViewBag.Title = "List";
    ViewBag.pTitle = "MC Payment Voucher Print";
    ViewBag.pageTitle = "MC Payment Voucher Print";
    Layout = "~/Views/Shared/_Layout.cshtml";
    var _decimal = @Session["Decimal"].ToString();
    var isadd = true;
    var ismodify = true;
    var isdelete = true;
    var isprint = true;
    var isview = true;
    string path = "/MCPaymentVoucher/Index";

    
}
<style>
    tfoot {
        display: table-header-group;
    }
</style>
<style>
    .w-6 {
        width: 6%;
    }

    .charegRow {
        background: #f8fbff;
    }

        .charegRow td {
            border: 0 !important;
        }

        .charegRow label {
            font-size: 14px !important;
        }

    .taxBlock .col-md-2, .taxBlock .col-md-3 {
        display: flex;
        flex-wrap: wrap;
        align-items: center;
    }

    .align_self {
        margin-top: 31px;
    }

    .w-12 {
        width: 12%;
    }

    .taxBlock label {
        margin: 0;
    }
</style>

<script type="text/javascript">

    //$(function () {
    //    $('#FromDate').datetimepicker({ format: 'DD-MM-YYYY' });

    //    $('#ToDate').datetimepicker({ format: 'DD-MM-YYYY' });

    //});

    $(document).ready(function () {
         var startdate = '@Session["FyearFrom"].ToString()';
        var enddate = '@Session["FyearTo"].ToString()';
        var sd = new Date(startdate.split('-')[1] + '-' + startdate.split('-')[0] + '-' + startdate.split('-')[2]);
        var ed = new Date(enddate.split('-')[1] + '-' + enddate.split('-')[0] + '-' + enddate.split('-')[2]);


        $('#dataTables-example tfoot th').each(function () {
                var title = $(this).text();
                $(this).html('<input type="text" style="width:100%" />');
            });


        var table = $('#dataTables-example').DataTable({
            "aaSorting": []
            //"dom": '<"top"i>rt<"top"flp><"clear">',
            //dom: "<'row'<'col-sm-3'l><'col-sm-3'f><'col-sm-6'p>>" +
            //       "<'row'<'col-sm-12'tr>>" +"<'row'<'col-sm-5'i><'col-sm-7'p>>",
            //"pagingType": "simple"
        });
        table.draw();
        table.columns().every(function () {
            var that = this;

            $('input', this.footer()).on('keyup change', function () {
                if (that.search() !== this.value) {
                    that
                        .search(this.value)
                        .draw();
                }
            });
        });

        $('#btndownload').click(function () {
            $('#hdnDownload').val('true');
            $('#btnreport').trigger('click');
            $('#hdnDownload').val('false');
        });

    });


</script>

    @if (TempData["SuccessMsg"] != null)
    {

        <script type="text/javascript">
                 $(document).ready(function () {
                     //$.notify("@TempData["SuccessMsg"]", "success");
                 });
        </script>
    }
    <fieldset>
        @using (Html.BeginForm("Index", "MCPaymentVoucher", FormMethod.Post, new { @id = "customerledger" }))
        {
            @Html.AntiForgeryToken()

            @Html.ValidationSummary(true, "", new { @class = "text-danger" })
        <div class="card">
            <div class="card-body">
                <div class="row">
                    <div class="col-md-2">
                        <div class="mb-2">
                            <label class="headinglabel">From Date</label>
                            <div class="docs-datepicker">
                                <div class="input-group">
                                    <input type="text" class="form-control docs-date" name="FromDate" id="FromDate" value="@Model.FromDate.ToShortDateString()"
                                           placeholder="Pick a From Date" autocomplete="off">
                                    <button type="button"
                                            class="btn btn-secondary docs-datepicker-trigger"
                                            disabled>
                                        <i class="mdi mdi-calendar" aria-hidden="true"></i>
                                    </button>
                                </div>
                                <div class="docs-datepicker-container"></div>
                            </div>
                        </div>
                    </div>
                    <div class="col-md-2">
                        <div class="mb-2">
                            <label class="headinglabel">To Date</label>
                            <div class="docs-datepicker">
                                <div class="input-group">
                                    <input type="text" class="form-control docs-date" name="ToDate" id="ToDate" data-date-format="dd/MM/yyyy" data-date-autoclose="true" data-provide="datepicker" value="@Model.ToDate.ToShortDateString()"
                                           placeholder="Pick a date" autocomplete="off">
                                    <button type="button"
                                            class="btn btn-secondary docs-datepicker-trigger"
                                            disabled>
                                        <i class="mdi mdi-calendar" aria-hidden="true"></i>
                                    </button>
                                </div>
                                <div class="docs-datepicker-container"></div>
                            </div>
                        </div>
                    </div>
                    <div class="col-md-2 search_field">
                        <label for="FromDate" class="headinglabel">Voucher No./ AWB No.</label>
                        <input type="hidden" id="hdnDownload" name="Download" value="false" />
                        @Html.TextBoxFor(mode => mode.AWBNo, new { @class = "form-control text-right blue_border" })
                    </div>
                    <div class="col-md-2">
                        <div class="mb-2">
                            <button type="submit" class="btn btn-success waves-effect waves-light filter btn_top" id="btnreport">
                                <i class="bx bx-revision mt-1" style="font-size: 21px;"></i>
                            </button>
                            <button type="button" id="btndownload" class="btn btn-success waves-effect waves-light filter btn_top ">
                                <i class="mdi mdi-file-excel font-size-18"></i>
                            </button>
                        </div>
                    </div>
                    
                    <div class="col-md-4 text-md-end btn_top">
                        @if (@Net4Courier.Models.CommonFunctions.CheckCreateEntryValid() == false || !isadd)
                        {
                            <a href='/MCPaymentVoucher/Create' class="btn btn-primary me-2" id="Create" disabled><i class="mdi mdi-plus me-1"></i> New MC Payment Voucher</a>
                        }
                        else
                        {
                            <a href='/MCPaymentVoucher/Create' class="btn btn-primary me-2" id="Create"><i class="mdi mdi-plus me-1"></i>New MC Payment Voucher</a>
                        }
                    </div>
                </div>
        </div>
        </div>

        <div class="card">
            <div class="card-body">
                 <table class="table table-bordered mb-0" id="datatable">
                    <thead class="table-light">
                        <tr>
                            <th>Print Date</th>
                            <th>Voucher No.</th>
                            <th>Consignor</th>
                            <th>Total AWB</th>
                            <th>Total Amount</th>
                            <th>MC Closed</th>
                            <th>Action</th>
                        </tr>
                    </thead>
                  
                    <tbody>
                        @foreach (var item in @Model.Details)
                        {
                            <tr>
                                <td style="text-align:right">
                                    @Html.DisplayFor(modelItem => item.PrintDate)
                                    
                                </td>
                                <td>
                                    @Html.DisplayFor(modelItem => item.VoucherNo)
                                </td>
                                <td>
                                    @Html.DisplayFor(modelItem => item.Consignor)
                                </td>
                                <td style="text-align:right">
                                    @Html.DisplayFor(modelItem => item.TotalAWB)
                                </td>
                                <td style="text-align:right">
                                    @Html.DisplayFor(modelItem => item.TotalAmount)
                                </td>
                                <td>
                                    @{ 
                                        if (item.Closed)
                                        {
                                    @item.EmployeeName <br />
                                    @Net4Courier.Models.CommonFunctions.GetShortDateFormat(item.ClosedDate)
                                            }
                                        }
                                </td>
                                <td>
                                <div class="d-flex gap-3">
                                    @if (ismodify)
                                    {
                                        <a href="@Url.Action("Edit", new { id = item.MCPaymentVoucherID })" class="text-success"><i class="mdi mdi-pencil font-size-18"></i></a>
                                    }
                                    @if (isdelete)
                                    {
                                        <a href="#" onclick="Delete(ID=@item.MCPaymentVoucherID)" class="text-danger"><i class="mdi mdi-delete font-size-18"></i></a>
                                    }
                                    @if (isprint)
                                    {
                                        <a class="aprint text-secondary" target="_blank" href="VoucherPrint?id=@item.MCPaymentVoucherID"><i class="fa fa-print"></i></a>
                                    }
                                    </div>
                                </td>
                            </tr>
                        }
                    </tbody>
                </table>
            </div>
</div>

        }
    </fieldset>


<script type="text/javascript">
    // function Delete(ID) {
    //     if (confirm("Are you sure, You want to Delete the Print Voucher?")) {
    //         window.location.href = "/MCPaymentVoucher/DeleteConfirmed?id=" + ID;
    //     }
    // }

    function Delete(ID) {
        Swal.fire({ title: "Are you sure?", text: "Do you want to delete this?", icon: "warning", showCancelButton: !0, confirmButtonColor: "#34c38f", cancelButtonColor: "#f46a6a", confirmButtonText: "Yes, delete it!" }).then(
            function (t) {
                if (t.value) {

                    $.ajax({
                        type: "POST",
                        url: '/MCPaymentVoucher/DeleteConfirmed',
                        datatype: "html",
                        data: {
                            'id': ID
                        },
                        success: function (response) {
                            if (response.status == "OK") {
                                Swal.fire("Delete Status!", response.message, "success");
                                window.location.reload();
                            }
                            else {
                                Swal.fire("Delete Status!", response.message, "error");
                            }
                        }
                    });

                }
            });

    }
</script>

