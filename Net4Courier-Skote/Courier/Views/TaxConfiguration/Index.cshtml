@model IEnumerable<Net4Courier.Models.TaxConfigurationVM>

@{
    ViewBag.Title = "List";
     ViewBag.pTitle = "Empost Fees Setup";
    ViewBag.pageTitle = "Empost Fees Setup";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<script type="text/javascript">
    $(document).ready(function () {
        $('#dataTables-example tfoot th').each(function () {
            var title = $(this).text();
            $(this).html('<input type="text" style=width:100%  />');
        });


         var table = $('#dataTables-example').DataTable({
                "aaSorting": []                
            });
            table.draw();

        table.columns().every(function () {
            var that = this;

            $('input', this.footer()).on('keyup change', function () {
                if (that.search() !== this.value) {
                    that
                        .search(this.value)
                        .draw();
                }
            });
        });

    });


</script>

<div class="clearfix"></div>

      @if (TempData["SuccessMsg"] != null)
    {
  
             <script type="text/javascript">
                 $(document).ready(function () {
                     $.notify("@TempData["SuccessMsg"]", "success");
                 });
                                                </script>
    }
   <div class="card">
        <div class="card-body">
                <div class="col-md-12 mb-3 text-md-end">
<a  class="btn btn-primary waves-effect waves-light text-md-end" href="/TaxConfiguration/Create"><i class="mdi mdi-plus me-1"></i> New Empost Fees Setup

</a>
</div>
       <table class="table table-bordered mb-0" id="datatable" style="width:100%">
                <thead class="table-light">
    <tr>
      
        <th>
          Effective Date
        </th>
        <th>
           Courier Mode

        </th>
        <th>
            Description
        </th>
        <th>
            Sale Head
        </th>
        <th>
            Minimum Rate
        </th>
           <th>
            Tax%
        </th>

        <th>Action</th>
    </tr>
        </thead>
    
@foreach (var item in Model) {
    <tr>
        <td class="text-right">
       @*     @Html.DisplayFor(modelItem => item.EffectFromDate)*@
                  @Net4Courier.Models.CommonFunctions.GetShortDateFormat(@item.EffectFromDate)
        </td>
      
        <td>
            @Html.DisplayFor(modelItem => item.Movement)
        </td>
       
        <td>
            @Html.DisplayFor(modelItem => item.CourierType)
        </td>
           <td>
            @Html.DisplayFor(modelItem => item.AcHead)
        </td>
           <td style="text-align:right">
            @Html.DisplayFor(modelItem => item.MinimumRate)
        </td>
           <td style="text-align:right">
            @Html.DisplayFor(modelItem => item.TaxPercentage)
        </td>
        <td>
        <div class="d-flex gap-3">   
             <a href="@Url.Action("Edit", new { id = item.TaxConfigurationID })" class="text-success"><i class="mdi mdi-pencil font-size-18"></i></a>
        <a href="#" onclick="Delete(ID=@item.TaxConfigurationID)" class="text-danger"><i class="mdi mdi-delete font-size-18"></i></a>
        </div>
        </td>
    </tr>
}

</table>
        </div>
    </div>

<script type="text/javascript">
    // function Delete(ID) {
    //     if (confirm("Are you sure, You want to Delete Tax Confiqration?")) {
    //         window.location.href = "/TaxConfiguration/DeleteConfirmed/?id=" + ID;
    //     }
    // }
    
            function Delete(ID) {
        Swal.fire({ title: "Are you sure?", text: "Do you want to delete this?", icon: "warning", showCancelButton: !0, confirmButtonColor: "#34c38f", cancelButtonColor: "#f46a6a", confirmButtonText: "Yes, delete it!" }).then(
            function (t) {
                if (t.value) {
                    // $.ajax({
                    //     type: "POST",
                    //     url: '/TaxConfiguration/DeleteConfirmed',
                    //     datatype: "html",
                    //     data: {
                    //         'id': ID
                    //     },
                    //     success: function (response) {
                    //         if (response.status == "OK") {
                    //             Swal.fire("Delete Status!", response.message, "success");
                    //             window.location.reload();
                    //         }
                    //         else {
                    //             Swal.fire("Delete Status!", response.message, "error");
                    //         }
                    //     }
                    // });
                }
            });

            }
</script>































